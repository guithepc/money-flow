telegram.bot-token=${TELEGRAM_BOT_TOKEN:REDACTED_TELEGRAM_TOKEN}
jwt.secret=${JWT_SECRET:REDACTED_JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:3600000}

spring.datasource.username=${DB_USER:pctheone}
spring.datasource.password=${DB_PASS:postgres}

#spring.datasource.url=jdbc:postgresql://localhost/money_flow
spring.datasource.url=jdbc:postgresql://${DB_HOST}:5432/${DB_NAME}

